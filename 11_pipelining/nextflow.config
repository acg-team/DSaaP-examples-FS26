params {
    input = 'data/*.fasta'
    outdir = 'nextflow_results'
}

manifest {
    name = 'PKS Analysis Pipeline'
    description = 'Pipeline for polyketide synthase analysis: FASTA → Alignment → Phylogenetic Analysis (raxml-ng and BEAST2)'
    version = '1.0.0'
    nextflowVersion = '>=25'
}

// ---------------------------------------------------------------------------
// Reports
// ---------------------------------------------------------------------------
timeline {
    enabled = true
    file = "${projectDir}/${params.outdir}/pipeline_info/timeline.html"
    overwrite = true
}
report {
    enabled = true
    file = "${projectDir}/${params.outdir}/pipeline_info/report.html"
    overwrite = true
}
trace {
    enabled = true
    file = "${projectDir}/${params.outdir}/pipeline_info/trace.txt"
    overwrite = true
}
dag {
    enabled = true
    file = "${projectDir}/${params.outdir}/pipeline_info/dag.html"
    overwrite = true
}

// ---------------------------------------------------------------------------
// Runner configuration
// ---------------------------------------------------------------------------
profiles {

    // Run locally with conda environment in .env/
    standard {
        process.executor = 'local'
        process.conda = "${projectDir}/.env"
        conda.enabled = true
        conda.useMicromamba = true
    }

    // Run on a SLURM cluster with a conda environment
    slurm {
        process.executor = 'slurm'
        process.queue = 'normal'
        process.clusterOptions = '--account=<your_account>'
        process.conda = "${projectDir}/.env"
        conda.enabled = true
        conda.useMicromamba = false
    }

    // Run on a SLURM cluster with a shared conda environment
    slurm_zhaw {
        process.executor = 'slurm'
        process.queue = 'earth-1'
        process.time = '24:00:00'
        process.memory = '32.GB'
        process.conda = "${projectDir}/.env"
        conda.enabled = true
        conda.useMicromamba = false
    }
}
